apiVersion: v1
kind: ConfigMap
metadata:
  name: rbtc-faucet-config
  namespace: rbtc-faucet-${ENVIRONMENT}
  labels:
    app: rbtc-faucet
    environment: ${ENVIRONMENT}
data:
  # Add your configuration here
  # Example:
  # APP_NAME: "RBTC Faucet"
  # LOG_LEVEL: "info"
